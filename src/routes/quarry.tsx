import { createFileRoute } from "@tanstack/react-router";
import { QuarryPrototype } from "../components/quarry-prototype";
export const Route = createFileRoute("/quarry")({ component: QuarryPrototype });
